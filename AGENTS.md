# 프로젝트 작업 규칙

## 대상과 보존

- Go/Fyne 데스크톱 타이핑 게임이다. 모바일·웹·콘솔 게임으로 가정하지 않는다.
- 원본 Go 코드, `fonts/`, `go.mod`, `go.sum`, 게임 설정과 Fyne 사용자 최고 기록을 보존한다.
- 출시본·패치 기준·롤백 패키지·심볼/매핑·서명 키·인증 정보는 필요성과 관리자의 보존 정책을 확인하기 전 삭제하지 않는다. `.git`과 프로젝트 밖의 공유 캐시를 정리 대상으로 삼지 않는다.

## 필수 빌드 정리

- 개발 빌드·패키징·테스트·실행은 `make build/package/test/run` 또는 `python3 -B scripts/build.py <명령>`을 사용한다. `make clean`은 소유 표식과 잠금을 확인한 복구 및 보관 제한 적용이다.
- 새 빌드/패키징 도구와 CI도 `scripts/build.py`의 잠금, 전용 작업 디렉토리, 종료 처리, 검증 후 게시, 보관 정책을 사용하도록 연결한다. 임의 경로를 받는 재귀 삭제나 `git clean -xfd`를 추가하지 않는다.
- 임시는 `.build/work/<작업 ID>/`에만 생성하고 `GOTMPDIR`, `TMPDIR`, `TMP`, `TEMP`를 전달한다. 백그라운드 분리/데몬화 빌드는 금지한다. 성공·실패·SIGINT/SIGTERM/SIGHUP 뒤 정리하고 강제 종료 잔여물은 다음 실행에서 잠금과 프로세스 그룹을 확인한 후 회수한다.
- 개발 성공 산출물은 최근 2개, 원문 로그는 최근 10개 작업·파일당 최대 1MiB(마지막 부분)를 보관한다. 출시 보관본·롤백 자료·심볼에는 이 제한을 적용하지 않는다.
- 새 산출물을 검증하고 완성된 디렉토리를 게시한 다음 현재 참조를 원자적으로 교체한다. 실패 시 이전 정상 참조를 보존한다. 정리 실패는 경고·잔여 경로·실패 종료 코드로 드러낸다.
- 사유 없이 날짜/번호별 패키지나 압축 해제 사본을 계속 복제하지 않는다. 디버깅 보관은 `docs/build-cleanup.md`의 예외 기록에 목적·위치·담당자·삭제 시점과 보존할 근거를 먼저 기록하고, 자동 관리 영역 밖에 최소 범위만 보관한다.
- 정리 변경 시 `make test-cleanup`으로 수명 주기, 동시 작업 보호, 게시 실패 및 보관 제한을 검증한다. 게임 코드를 변경하면 `make test`도 수행한다. 자세한 정책: [docs/build-cleanup.md](docs/build-cleanup.md).
